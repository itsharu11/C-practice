/*
	variables:
		1)Global variable
			- whole program can access it.

		2)local variable
			- it is used in functions only.
			-local variables overwrites the values of global variables.



*/
#include<stdio.h>
#include<conio.h>

#define c 10    // constant variable
#define d 20
//int a=10,b=20;
//int a,b;
int main()

{
	int a,b;
       //	int a;
	clrscr();
	printf("enter two numbers ");
	scanf("%d %d",&a,&b);
	// scanf requires & as it stores value in it and
	//printf only fetches the value from it.
	printf("a=%d b=%d",a,b);
	getch();
	return 0;
}